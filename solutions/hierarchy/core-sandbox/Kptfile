apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: hierarchy-core-sandbox
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: A sandbox hierarchy that assumes that a distinct GCP org has been created specifically for sandbox
pipeline:
  mutators:
    - image: gcr.io/kpt-fn/apply-setters:v0.2
      configPath: setters.yaml