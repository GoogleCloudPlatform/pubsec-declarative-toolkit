apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: federal-policy
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: Constraint Bundle for enabling Unclassified policy. Depends on `gatekeeper-policies` package.
pipeline:
  mutators:
    - image: gcr.io/kpt-fn/set-enforcement-action:v0.1.0
      configMap:
        enforcementAction: dry-run # change to `deny` to enforce policy