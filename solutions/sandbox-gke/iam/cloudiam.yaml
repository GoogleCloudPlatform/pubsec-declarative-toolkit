apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMPartialPolicy
metadata: # kpt-merge: ${sandbox-namespace}/kcc-${sandbox-project-id}-owners-permissions
  name: kcc-sandbox-project-id-owners-permissions # kpt-set: kcc-${sandbox-project-id}-owners-permissions
  namespace: sandbox # kpt-set: ${sandbox-namespace}
  annotations:
    cnrm.cloud.google.com/project-id: config-controller-project-id # kpt-set: ${management-project-id}
spec:
  resourceRef:
    apiVersion: resourcemanager.cnrm.cloud.google.com/v1beta1
    kind: Project
    external: sandbox-project-id # kpt-set: ${sandbox-project-id}
  bindings:
    - role: roles/owner
      members:
        - member: serviceAccount:sandbox-project-id@config-controller-project-id.iam.gserviceaccount.com # kpt-set: serviceAccount:${sandbox-project-id}@${management-project-id}.iam.gserviceaccount.com
