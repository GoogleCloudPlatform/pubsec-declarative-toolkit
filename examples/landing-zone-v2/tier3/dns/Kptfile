apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: example-dns
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: |
    Landing zone v2 tier3 resource example.
    Depends on `client-project-setup`.

    Example to create a DNS address record (A), which maps a host name to an IPv4 address hosted in a client's project (tier3 namespace).
pipeline:
  mutators:
    - image: gcr.io/kpt-fn/apply-setters:v0.2
      configPath: setters.yaml
