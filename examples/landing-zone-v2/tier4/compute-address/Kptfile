apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: example-compute-address
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: |
    Landing zone v2 tier4 resource example.
    Depends on `client-project-setup`.

    Example to create an IPv4 compute address hosted in a client's project (tier4 namespace).
pipeline:
  mutators:
    - image: gcr.io/kpt-fn/apply-setters:v0.2
      configPath: setters.yaml
